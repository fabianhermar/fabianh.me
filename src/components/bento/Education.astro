---
import Arrow from '@/components/icons/Arrow.astro'
import { sanitizeHtml } from '@/helpers/satinizeHTML'
import { getI18N } from '@/i18n'

const { currentLocale } = Astro
const i18n = getI18N({ currentLocale })
---

<div class='py-4 md:py-0'>
	<a href='https://utna.edu.mx' target='_blank' rel='noopener noreferrer'>
		<div class='group relative overflow-hidden transition-all duration-300 md:p-4'>
			<div class='mb-2 flex items-center justify-between px-4 md:px-0'>
				<h3
					class='font-heading w-full pr-2 text-lg font-bold text-black transition-colors md:text-3xl'
				>
					{i18n.landing.bento.education.career}
				</h3>
				<Arrow
					class:list='h-6 w-6 flex-shrink-0 transition-all duration-300 group-hover:-translate-y-1 group-hover:translate-x-1'
					styles='text-black'
				/>
			</div>
			<time class='font-display mb-4 px-4 text-neutral-500 md:px-0'
				>{i18n.landing.bento.education.location} | {i18n.landing.bento.education.duration}</time
			>
		</div>
	</a>
	<p
		class='font-accent px-4 pt-4 text-lg text-pretty text-neutral-800'
		set:html={sanitizeHtml(i18n.landing.bento.education.description)}
	/>
</div>
